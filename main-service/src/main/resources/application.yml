server:
  port: 8081

spring:
  application:
    name: main-service
  config:
    # Используем optional, чтобы не падать сразу, если config-service недоступен
    import: "optional:configserver:${CONFIG_URI:http://localhost:8888}"
  cloud:
    config:
      fail-fast: false
      retry:
        enabled: true
        initial-interval: 1000
        max-attempts: 5

  # === FALLBACK DATASOURCE (ЗАПАСНАЯ КОНФИГУРАЦИЯ) ===
  datasource:
    url: jdbc:postgresql://localhost:5433/clinicdb
    username: clinic_user
    password: supersecret
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# === ВАЖНО: ОТКЛЮЧАЕМ EUREKA В ЛОКАЛЬНОМ КОНФИГЕ ===
# Это предотвратит падение приложения, если Config Service недоступен
eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

application:
  minio:
    url: http://localhost:9000
    access-key: minio_admin
    secret-key: minio_password
    bucket: medical-records
  jwt:
    secret: super-secret-key-that-must-be-very-long-and-secure-base64-encoded

management:
  endpoints:
    web:
      exposure:
        include: prometheus,health,info,metrics
  zipkin:
    tracing:
      endpoint: "http://localhost:9411/api/v2/spans"
  tracing:
    sampling:
      probability: 1.0